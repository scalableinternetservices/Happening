<ul class="nav nav-tabs">
  <li role="presentation" class="active"><a href="/">Attending</a></li>
  <li role="presentation"><a href="/users/:id/events">Hosting</a></li>
</ul>
<div class="attending">
  <h1>
    Events you're attending:
  </h1>

  <% if current_user.occasions.count == 0 %>
  <h4>
    You're not attending any events.
  </h4>
  <%= link_to 'Find one now!', events_path, class: "btn btn-primary btn-sm" %>
  <% else %>
  <div class="row">
      <% current_user.occasions.each do |event| %>
      <!--<% cache ['occasion_list', event] do %>-->
        <div class="col-sm-4 col-md-4 col-lg-4">
          <div class="panel panel-default">
            <div class="panel-heading">
              <h4><%= event.name %> <small>Hosted by <%= event.host %></small></h4>
            </div>
            <div class="panel-body">
              <p>
                <span class="glyphicon glyphicon-map-marker"></span>
                <%= event.location %>
              </p>
              <p>
                <span class="glyphicon glyphicon-time"></span>
                <%= event.timestart.strftime("%B %-d, %l:%M %p") %>
              </p>
              <p><%= link_to 'View', event, class: "btn btn-primary btn-sm" %> <%= link_to 'Edit', edit_event_path(event), class: "btn btn-default btn-sm" %></p>
            </div>
          </div>
        </div>
      <!--  <% end %>-->
      <% end %>
  </div>
  <%= link_to 'Find events', events_path, class: "btn btn-primary btn-sm" %>
  <% end %>
</div>
